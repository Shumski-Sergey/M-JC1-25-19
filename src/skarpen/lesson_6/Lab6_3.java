package src.skarpen.lesson_6;

import java.lang.reflect.Array;
import java.util.ArrayList;
import java.util.Comparator;
import java.util.Scanner;

public class Lab6_3 {
    public static void main(String[] args) {
        //Ввести n строк с консоли. Вывести на консоль те строки, длина которых меньше (больше) средней, а также длину.
       /* ArrayList<String> far = new ArrayList<>();
        Scanner sc = new Scanner(System.in);
        boolean pip = true;
        do {
            System.out.println("Input string:");
            far.add(sc.nextLine());
            System.out.println("continue: Y/No ?");
            if (!sc.nextLine().equalsIgnoreCase("Y")) {
                pip = false;
            }
        }
        while (pip);
        {
            int f = 0;
            for (int i = 0; i < far.size(); i++) {
                f += far.get(i).length();
            }
            int srNum = f / far.size();
            System.out.println("Сумма длин элементов массива - " + f + "\n" + "Средняя длина элементов массива - " +srNum + '\n' + "Количество элементов в массиве - " + far.size());
            for (int j = 0; j < far.size(); j++) {
                if(far.get(j).length()>srNum) {
                    System.out.println("Значения длин элементво больше среднего значения длины массива - " + j + "Сама строка - " + far.get(j));
                }
            }
        }
*/
    }
}


