package src.skarpen.lesson_6;

import java.util.Scanner;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Lab6_2_3 {
    public static void main(String[] args) {
        //Имеется строка с текстом. Вывести текст, составленный из последних букв всех слов
       /*
        System.out.println("Input text:");
        Scanner scanner = new Scanner(System.in);
        String str = scanner.nextLine();
        String string = "([а-я[А-Я]]+)|([a-z[A-Z]]+)";
        StringBuilder stringBuilder = new StringBuilder();

        Pattern pattern = Pattern.compile(string);
        Matcher matcher = pattern.matcher(str);

        while (matcher.find()) {
            stringBuilder.append(str.substring(matcher.end()-1, matcher.end()));
        }
        System.out.println(stringBuilder);
        */
    }
}

