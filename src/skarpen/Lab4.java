package src.skarpen;

/**
 * Created by user on 21.12.2019.
 */
public class Lab4 {
    public static void main(String[] args){
/*
        float number;
        do {
            Scanner sc = new Scanner( System.in );
            System.out.println( "Please enter number!" );
            while (!sc.hasNextInt()) { // 'Вещь !!Запомнить!!!
                System.out.println( "That's not a number!" );
                sc.next(); // this is important!
            }
            number = sc.nextFloat();
            if (number < 0) {
                System.out.println( "You entered a number less than 0" );
            } else {
                long resuit = 1;
                for (int i = 2; i <= number; i++)
                    resuit *= i;
                System.out.println( resuit );
                if (number == 0 || number == 1)
                    System.out.println( "factorial numbers 0 and 1 equall 1" );
            }
        } while (number < 0);
        System.out.println( "Congratulations" );

  int num;
        Scanner scan = new Scanner( System.in );
        System.out.println( "Введите натуральное число" );
        if (scan.hasNextInt()) {
            num = scan.nextInt();
            System.out.print( "Делителями числа " + num + " являются " );
            for (int i = num; i > 0; i--) {
                int b = num % i;
                if (b == 0)
                    System.out.print( i + " " );
            }
        } else System.out.println( "Ошибка. Введено не число" );
        */
    }}
