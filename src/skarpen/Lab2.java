package src.skarpen;


/**
 * Created by user on 18.12.2019.
 */
public class Lab2 {
    public static void main(String[] args) {
        /*
        ///--abs - метод опредлеяет число по модулю!--/
        double n = -11;
        double m = -10;
        if ((abs(10 - n)) > (abs(10 - m))) {
            System.out.println(m + " ближе к 10.0");
        } else {
            System.out.println(n + " ближе к 10.0");
        }
        int a = 1;
        int b = 2;
        int c = 3;
        int function = a * (x ^ 2)+ b * x + c; //- Убейте меня - квадратный ТРЕХчлен!

/*
        double a = 156;
        double b = 123891;
        double c = -5;
        double minVar = Math.min(Math.min(a, b), c);
        System.out.println(minVar + " - Минимальное число");
        double maxVar = Math.max(Math.max(a,b),c);
        System.out.println(maxVar + " - Наибольшее число");
        double medium = (a+b+c) - (maxVar+minVar);
        System.out.println(minVar + " " + medium + " " + maxVar + " - числа в порядке возростания" );
//  Math.random()*(b-a) ) + a- формула определения диапозона
        //   [5;155]
        int a = 25;
        int b = 100;
        int c = (int) (Math.random() * (155 - 5) + 5);
        if (c > 25 & c< 100){
            System.out.println(c + "\n" + "Число выбранное рандомно попало в необходимый диапозон [25;100]");
        } else {
            System.out.println(c +  "\n" + "Число не попало в диапозон [25;100]");
        };
for (int i = 5; i>=1; i--){
    System.out.print(i + " ");
}
*/
/*
        Scanner scanner = new Scanner(System.in);   //Определяет данные на вход!
        System.out.println("Ввидите положительное число!");
        int a = scanner.nextInt(); // nextInt и nextLine - выводит число либо строку соответственно
        if (a < 0) {
            System.out.println("Ск! Cказали, же ввести положительное число!");
        } else {
            for (int i = 0; i <= a; i++) {
                int sum = i * (i + 1) / 2; // формула арифмитической проггрессии , взял с интернета не придумал сам
                System.out.println(sum);
            }

        }
*/
    }
}
